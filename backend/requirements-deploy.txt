# What the server actually needs, as opposed to requirements.txt, which is the
# full local environment. Nothing is removed from the app - the embedding model
# is the same all-MiniLM-L6-v2 as before, but run through onnxruntime instead of
# PyTorch, which returns the same vectors (cosine similarity 1.0000000000) for a
# fraction of the cost. See implementations/embeddings.py.
#
# That drops torch, transformers and sentence-transformers, and with them 2.7 GB
# of NVIDIA CUDA drivers a CPU-only host could never use. onnxruntime and
# tokenizers arrive as chromadb dependencies, so nothing is added to replace it.

django==6.1
djangorestframework==3.18.0
django-cors-headers==4.9.0
gunicorn==23.0.0
python-dotenv==1.2.2

langchain==1.3.15
langchain-core==1.5.4
langchain-chroma==1.1.0
langchain-groq==1.1.3
langchain-ollama==1.1.0
langchain-text-splitters==1.1.2
chromadb==1.5.9
groq==0.37.1

numpy==2.5.2
